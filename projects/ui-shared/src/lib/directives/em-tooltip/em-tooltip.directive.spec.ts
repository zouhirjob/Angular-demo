import { EmTooltipDirective } from './em-tooltip.directive';

describe('EmTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new EmTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
